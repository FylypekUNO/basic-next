export const page = {
  home: '/',
  signIn: '/auth/signin',
};

export const api = {
  signUp: '/api/auth/signup',
};

// For dynamic paths write using functions e.g.
//  user: (id: string) => `/user/${id}`,
